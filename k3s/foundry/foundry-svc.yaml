apiVersion: v1
kind: Service
metadata:
  name: foundry
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app.kubernetes.io/name: foundry
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376

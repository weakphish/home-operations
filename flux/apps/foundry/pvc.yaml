apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: foundry-data-claim
  namespace: default
spec:
  storageClassName: longhorn
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 50Gi

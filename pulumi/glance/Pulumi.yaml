name: glance
description: Glance dashboard with Tailscale ingress for homelab overview
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-typescript

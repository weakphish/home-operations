name: cf-connector
description: Cloudflare tunnel connector deployment (runs cloudflared pods)
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-typescript
